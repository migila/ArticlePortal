import { NgModule } from '@angular/core';
import { UserService } from './user.service';

@NgModule({
    declarations: [
    ],
    imports: [
    ],
    providers: [
      UserService
      ],
  })
  export class CoreModule { }
